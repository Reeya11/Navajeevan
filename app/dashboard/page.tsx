// In your dashboard/page.tsx - TEMPORARY SIMPLE VERSION
export default function DashboardPage() {
  const userStats = {
    totalListings: 7, // Hardcode your 7 items for now
    activeListings: 7,
    totalMessages: 0,
    soldItems: 0
  }
}
  // Remove all the useEffect, useState, loading states for now
  // Just return the UI with hardcoded data